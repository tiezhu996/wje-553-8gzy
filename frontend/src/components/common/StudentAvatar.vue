<script setup lang="ts">
import type { Student } from '../../types/student';
withDefaults(defineProps<{ student?: Pick<Student, 'name' | 'student_no' | 'class_name'>; fallback?: string }>(), { fallback: '' });
</script><template><div class="student" v-if="student"><el-avatar :size="32">{{ student.name.slice(0,1) }}</el-avatar><span><b>{{ student.name }}</b><small>{{ student.student_no }} · {{ student.class_name || '-' }}</small></span></div><span v-else>{{ fallback }}</span></template><style scoped>.student{display:flex;align-items:center;gap:10px}.student span{display:grid}.student small{color:#64748b}</style>